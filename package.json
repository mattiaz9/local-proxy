{
  "name": "localproxy",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "cors-anywhere": "^0.4.4",
    "dotenv": "^10.0.0",
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "dev": "nodemon --watch index.js",
    "start": "pm2 start index.js && pm2 save"
  }
}
